# validCitiesPSMSLeads
This script automates the process of reading an email with a CSV attachment, extracting the relevant data based on city and condition rules, formatting key date fields, and updating two separate sheets inside a spreadsheet. 
